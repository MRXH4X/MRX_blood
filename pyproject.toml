[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mrx-blood"
version = "1.4.4"
description = "MRX Blood - BloodHound Attack Path Analyzer"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
dependencies = [
    "flask>=3.0.0",
]

[project.optional-dependencies]
production = [
    "waitress>=3.0.0",
]

[project.scripts]
mrxblood = "bh_analyzer.__main__:main"
bh-analyzer = "bh_analyzer.__main__:main"

[tool.setuptools]
packages = ["bh_analyzer"]
